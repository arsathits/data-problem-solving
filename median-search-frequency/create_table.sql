-- Create table
CREATE TABLE msf_search_frequency (
    searches INT,
    num_users INT
);

-- Insert sample data
INSERT INTO msf_search_frequency (searches, num_users) VALUES
(1, 2),
(2, 2),
(3, 3),
(4, 1);
